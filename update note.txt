26.02.10
I2C传输速度从100khz下调至50khz
show串口命令增加开关状态展示
修改App_SYS初始化printf内容

26.02.16
原代码中，如只修改湿度阈值，则初始化时，if (bkp_t_bot != 0.0f || bkp_t_top != 0.0f) 判断为假，程序执行了 else 分支，直接加载了硬编码的默认值，覆盖了写入 BKP 的湿度数据。加入魔术数，各阈值独立写入。

26.04.05
“集成stm32”pcb：
当前代码外部晶振配置为bypass

26.04.07
排除pcb设计中NRST的硬件设计错误。
修改I2C传输速度从50khz下降至10khz。
上拉电阻由4.7k减小至2k。
I2C通信失败无法修正。

26.04.14
使用了优化I2C走线的PCB，I2C通信仍然失败。
尝试调整I2C传输速度10k上升至50k。通信失败。

26.04.15
在260415_Simu_I2C项目中尝试使用模拟I2C，正常完成通信。